package processors

// internal-env-keys
const (
	RestEnvTag = `rest-env-tag`
)

// internal-env-keys

// external-env-keys
const ()

// rest env external olarak içeri aktarılacak env map içerisinde barınan keys doğrulamak için reference alınacak slice
var RestEnvKeyRefSlice []string = []string{}

// external-env-keys
